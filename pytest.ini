[pytest]
# The documented command is `pytest` from the repo root. Without this, pytest
# also walks scripts/, where diagnostic tools such as test_yfinance_delay.py
# are named test_* but are command-line utilities, not tests.
testpaths = tests
